name: Build Modules

on:
  push:
    tags:
    - '*'

env:
  NODE_VERSION: "20.x"

jobs:
  build:
    name: Build Modules
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      max-parallel: 1
      matrix:
        os: [ macos-latest, ubuntu-latest, windows-latest, ubuntu-24.04 ]

    steps:
      - uses: actions/checkout@v4

      - name: Setup NodeJS
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        if: matrix.os != 'ubuntu-24.04'
        run: npm install

      - name: Install node-tgz-downloader
        if: matrix.os == 'ubuntu-24.04'
        run: npm install -g node-tgz-downloader

      - name: Download tarball
        if: matrix.os == 'ubuntu-24.04'
        run: download-tgz package-json --directory tarballs package.json && download-tgz package-lock --directory tarballs package-lock.json

      - name: Create module zip
        uses: thedoctor0/zip-release@0.7.6
        with:
          type: 'zip'
          filename: "release-${{ matrix.os }}.zip"
          exclusions: '*.git*'

      - name: Upload Release
        uses: ncipollo/release-action@v1.14.0
        with:
          artifacts: "release-${{ matrix.os }}.zip"
          token: ${{ secrets.GITHUB_TOKEN }}
          allowUpdates: true
          replacesArtifacts: true
